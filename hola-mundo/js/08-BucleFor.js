'use strict'

// Bucle FOR.
// Loops.
// Es una estructura de control que se repite varias veces

var numero = 100;

for( let i = 0 ; i <= numero ; i++ ) {
  console.log( `Va en el número: ${i}` ); 

  debugger;
};